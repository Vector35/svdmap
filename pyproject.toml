[tool.poetry]
name = "svdmap"
version = "0.1.6"
description = ""
authors = ["Vector35"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
svd2py = "1.0.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
